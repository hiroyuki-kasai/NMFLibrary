% Add folders to path.

addpath(pwd);

cd solver/;
addpath(genpath(pwd));
cd ..;

cd solver_new/;
addpath(genpath(pwd));
cd ..;

cd auxiliary/;
addpath(genpath(pwd));
cd ..;

cd plotter/;
addpath(genpath(pwd));
cd ..;

cd data/;
addpath(genpath(pwd));
cd ..;

nmflibrary_message();


